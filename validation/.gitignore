# Regenerable outputs and caches -- not source. Cache tables can be
# individually gigabytes (a full/large-tier fine table), and the figs/data
# get regenerated by re-running the scripts; committing them here would just
# bloat the repo with binary artifacts that go stale the moment params.py
# changes.
data/
figs/
*.log
*.sync-conflict-*
